type Hour = number;
type GeoPoint = {
  latitude: number;
  longitude: number;
};
type Markdown = string;
type GeoJson = string;
type Km = number;

export type { GeoJson, GeoPoint, Hour, Km, Markdown };
